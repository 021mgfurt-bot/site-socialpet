# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Variáveis de ambiente locais — nunca versionar, mesmo que hoje nenhuma
# contenha segredo (Prompt 15 §19). .env.example continua rastreado de
# propósito, é só placeholder público.
.env
.env.local
.env.*.local
.env.production

# Artefatos de QA (screenshots automatizadas) — não fazem parte do site
qa/screenshots

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
